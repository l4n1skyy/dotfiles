# --- AUTO-GENERATED ---

# Workspace 1
exec-once = [workspace 1] sh -c '[ ! -f $HOME/.cache/disable_autostart ] && uwsm app -- alacritty --class org.omarchy.nvim -e nvim'
exec-once = [workspace 1 silent] sh -c '[ ! -f $HOME/.cache/disable_autostart ] && uwsm app -- alacritty --class timr-term -e timr'
exec-once = [workspace 1 silent] sh -c '[ ! -f $HOME/.cache/disable_autostart ] && uwsm app -- alacritty --class cbonsai-term -e cbonsai -i -l'

# Workspace 2
exec-once = [workspace 2 silent] sh -c '[ ! -f $HOME/.cache/disable_autostart ] && uwsm app -- zen-browser'

# Workspace 9
exec-once = [workspace 9 silent] sh -c '[ ! -f $HOME/.cache/disable_autostart ] && uwsm app -- omarchy-launch-webapp https://calendar.google.com'
exec-once = [workspace 9 silent] sh -c '[ ! -f $HOME/.cache/disable_autostart ] && uwsm app -- obsidian'
exec-once = [workspace 9 silent] sh -c '[ ! -f $HOME/.cache/disable_autostart ] && uwsm app -- chromium --app=https://app.todoist.com/app --user-data-dir="/home/l4n1skyy/.config/todoist-app"'

# Workspace 10
exec-once = [workspace 10 silent] sh -c '[ ! -f $HOME/.cache/disable_autostart ] && uwsm app -- vesktop'
exec-once = [workspace 10 silent] sh -c '[ ! -f $HOME/.cache/disable_autostart ] && uwsm app -- beeper'
exec-once = [workspace 10 silent] sh -c '[ ! -f $HOME/.cache/disable_autostart ] && uwsm app -- spotify'

exec-once = /home/l4n1skyy/.config/hypr/layout/layout.sh
